enum RoutineCategory { sleep, movement, food, mind, social, work, health, reflection }
enum EntryStatus { planned, done, skipped, partial, unknown }
enum TimeBucket { morning, midday, evening, custom }
enum BadgeType { win, consistency, insight }

/// Wellness-focused group goal categories shown in browse / filter UI.
enum WellnessGoal {
  sleep,
  weightLoss,
  anxiety,
  dementiaRisk,
  heartHealth,
  chronicPain,
  diabetes,
  mentalWellbeing,
  generalFitness,
  nutrition,
}

/// Post types inside a Wellness Group feed.
enum GroupPostType { activityLog, shareWin, question }
